apiVersion: v1
kind: Service
metadata:
    name: wil42-deployment
    namespace: dev
spec:
    type: NodePort
    selector:
        app: wil42
    ports:
        - port: 8888
          targetPort: 8888
          nodePort: 30888
